# Requirements for video generation functionality
# Install with: pip install -r requirements_video.txt

# Core dependencies (from main requirements.txt)
anthropic>=0.7.0
selenium>=4.15.0
Pillow>=9.0.0
python-dotenv>=0.19.0

# Video generation specific dependencies
opencv-python>=4.8.0
numpy>=1.21.0

# Optional: For better video encoding support
# ffmpeg-python>=0.2.0

# For better MP4 compression (if available)
# imageio[ffmpeg]>=2.25.0 