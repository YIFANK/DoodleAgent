# Lightweight requirements for video generation (no OpenCV!)
# Install with: pip install -r requirements_video_lite.txt

# Core dependencies (from main requirements.txt)
anthropic>=0.7.0
selenium>=4.15.0
Pillow>=9.0.0
python-dotenv>=0.19.0

# Lightweight video generation dependencies
imageio[ffmpeg]>=2.25.0
numpy>=1.21.0

# Alternative: If you prefer moviepy (also lightweight)
# moviepy>=1.0.3 